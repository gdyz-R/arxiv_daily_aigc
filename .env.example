# Copy this file to .env and fill in local secrets.
# The real .env is ignored by Git. GitHub Actions uses repository Secrets instead.
DEEPSEEK_API_KEY=
DASUAPI_API_KEY=
SEMANTIC_SCHOLAR_API_KEY=
# Optional, non-secret contact email used for the OpenAlex polite pool.
OPENALEX_MAILTO=